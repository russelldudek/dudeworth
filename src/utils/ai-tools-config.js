// Set this to true to enable AI Tools section and links
export const AI_TOOLS_ENABLED = true;

// Set this to true to enable the CSV uploader
export const CSV_UPLOADER_ENABLED = true;

console.log('AI_TOOLS_ENABLED:', AI_TOOLS_ENABLED);
console.log('CSV_UPLOADER_ENABLED:', CSV_UPLOADER_ENABLED);